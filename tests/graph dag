from src.graph import Graph


def test_graph():

    graph = Graph()

    graph.tambah_mk("INF101")

    graph.tambah_prasyarat(
        "INF101",
        "INF201"
    )

    assert "INF101" in graph.graph


print("Test Graph berhasil")
